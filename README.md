# BuidAI (必定AI) - 企业级 AI 应用开发平台模版

BuidAI (必定AI) 是一个基于 **Nuxt** 构建的现代化前端项目模版，专为 AI 应用开发平台、SaaS 产品官网及文档中心设计。项目集成了最新的 Web 技术栈，提供卓越的性能、SEO 优化和开发体验。

## ✨ 核心特性

- **🚀 最新技术栈** - 基于 **Nuxt 4** (兼容最新生态) 和 **Vue 3** 构建，享受极速的开发和构建体验。
- **🎨 现代化 UI** - 集成 **Tailwind CSS**，预设企业级配色方案（Primary, Accent, Secondary），支持 **Dark Mode**（深色模式）。
- **📚 内容驱动** - 深度集成 **Nuxt Content v3**，通过 Markdown (`.md`) 文件轻松管理文档 (`/docs`) 和博客 (`/blog`)。
- **🧩 组件化架构** - 封装丰富的功能组件（如 `HeroSection`, `FeatureCarousel` 等），基于 **Headless UI** 和 **Heroicons** 构建无障碍交互。
- **📱 全响应式** - 移动优先设计，完美适配 Mobile, Tablet, Desktop 等各种屏幕尺寸。
- **🔍 SEO 最佳实践** - 内置 Meta 标签管理、Open Graph 支持及结构化数据配置。

## 📂 目录结构

```text
e:\buidai.com\buidai/
├── assets/             # 静态资源
│   └── css/            # 全局样式 (main.css)
├── components/         # Vue 组件 (自动导入)
│   ├── landing/        # 首页/落地页专用组件
│   ├── content/        # Nuxt Content 渲染组件
│   └── ...             # 通用组件 (AppNavigation, AppFooter)
├── content/            # 内容数据源 (Markdown)
│   ├── blog/           # 博客文章
│   └── docs/           # 文档中心 (支持多级目录)
├── layouts/            # 页面布局
│   └── default.vue     # 默认布局
├── pages/              # 页面路由
│   ├── index.vue       # 首页
│   ├── docs/           # 文档中心路由 ([...slug].vue)
│   ├── blog/           # 博客路由 ([...slug].vue)
│   └── ...             # 其他页面 (pricing, solutions 等)
├── public/             # 公共静态文件 (图片, favicon)
├── app.vue             # 应用根组件
├── nuxt.config.ts      # Nuxt 核心配置
├── tailwind.config.js  # Tailwind 样式配置
└── package.json        # 依赖管理
```

## 🚀 快速开始

### 环境要求

- **Node.js**: 推荐 v18.0.0 或更高版本
- **包管理器**: npm, yarn 或 pnpm

### 安装依赖

```bash
npm install
```

### 启动开发服务器

启动本地开发环境，支持热更新 (HMR)：

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

### 构建生产版本

生成静态站点 (SSG) 或服务端渲染应用：

```bash
npm run build
```

构建产物将输出到 `.output/` 目录。

### 本地预览

预览构建后的生产版本：

```bash
npm run preview
```

### 类型检查

运行 TypeScript 类型检查：

```bash
npm run typecheck
```

## 🛠 技术栈概览

| 类别 | 技术 | 说明 |
|Data | Tech | Description |
| --- | --- | --- |
| **框架** | [Nuxt](https://nuxt.com/) | 全栈 Vue 框架，提供 SSR/SSG 能力 |
| **语言** | [TypeScript](https://www.typescriptlang.org/) | 强类型 JavaScript 超集 |
| **样式** | [Tailwind CSS](https://tailwindcss.com/) | 实用优先的 CSS 框架 |
| **图标** | [Heroicons](https://heroicons.com/) | 优雅的 SVG 图标库 |
| **组件库** | [Headless UI](https://headlessui.com/) | 无样式、无障碍的基础 UI 组件 |
| **内容管理** | [Nuxt Content](https://content.nuxt.com/) | 基于 Git 的 Headless CMS |
| **状态管理** | `useState` | Nuxt 内置状态管理 |

## ⚙️ 定制化指南

### 1. 修改主题颜色

在 `tailwind.config.js` 中可以自定义品牌色。项目预设了基于 Indigo/Violet 的配色：

```javascript
// tailwind.config.js
theme: {
  extend: {
    colors: {
      primary: { ... },   // 主品牌色
      accent: { ... },    // 强调色/辅助色
      secondary: { ... }, // 中性色/背景色
    }
  }
}
```

### 2. 配置 SEO 信息

全局 SEO 信息（如网站标题、描述）可以在 `nuxt.config.ts` 的 `app.head` 中修改：

```typescript
// nuxt.config.ts
app: {
  head: {
    title: '必定AI - 您的标题',
    meta: [
      { name: 'description', content: '您的网站描述' }
    ]
  }
}
```

页面级 SEO 可以使用 `useSeoMeta` 在各个 `.vue` 文件中单独配置。

### 3. 管理文档与博客

所有内容文件位于 `content/` 目录下：

- **文档**: 在 `content/docs/` 下创建 Markdown 文件。支持多级目录，可通过 `.navigation.yml` 配置侧边栏排序。
- **博客**: 在 `content/blog/` 下创建 Markdown 文件。文件头部 (Frontmatter) 支持设置标题、日期、封面图等。

示例 Frontmatter:

```yaml
---
title: '文章标题'
description: '文章简介'
date: '2024-03-20'
image: '/blog/cover.jpg'
---
```

## 🤝 贡献

欢迎提交 Issues 和 Pull Requests 来改进本项目。

## 📄 许可证

[MIT License](LICENSE)
