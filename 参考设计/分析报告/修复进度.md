# 修复优先级清单 - 执行状态

> 执行日期：2026-03-28

---

## ✅ 已修复

### 1. utils/pluginData.ts — 重复的应用 ID + 无效分类 ✅

**修复内容：**
- 第二个 `id: 5` (AI 证件照) 已改为 `id: 6`
- 后续所有 ID 已递增 (6→7, 7→8, etc.)
- 分类 `'漫剧创作'` 已改为 `'video'`

**验证：**
```bash
✅ 只有一个 id: 5
✅ 无"漫剧创作"分类
```

---

## ⏳ 待修复（需要手动处理）

### 2. 产品页 OG 图片大量 404

**影响文件：** `pages/product/*.vue` (10 个文件)

**问题：** 引用了不存在的图片文件

**修复建议：**
1. 为每个产品创建对应的封面图
2. 或指向已存在的截图（如 `-1.png`）

---

### 5. 购买按钮无任何交互逻辑

**文件：** `pages/plugin.vue`

**问题：** 购买按钮未绑定 click 事件

**修复建议：** 绑定 `openQrModal('wechat')` 联系客服

---

### 7. AOS 在 buidai.vue 中重复初始化

**文件：** `app.vue`, `pages/buidai.vue`

**问题：** 重复初始化 AOS，参数不一致

**修复建议：** 删除 `buidai.vue` 中的 AOS 导入和初始化

---

### 8. process.dev 已弃用

**文件：** `error.vue`

**修复：** `process.dev` → `import.meta.dev`

---

### 9. window.pageYOffset 已废弃

**文件：** `components/BackToTop.vue`

**修复：** `pageYOffset` → `scrollY`

---

### 10. Tailwind 配置版本不一致

**文件：** `package.json`, `tailwind.config.js`

**问题：** v4 使用了 v3 配置语法

**影响：** 自定义配置可能未生效

---

### 14. 图片文件名包含中文字符

**文件：** `public/` 目录

**建议：** 重命名为英文/拼音

---

### 15. solutions.vue 完全缺少暗色模式支持

**文件：** `pages/solutions.vue`

**建议：** 补充 `dark:` 样式类

---

## 验证命令

```bash
# TypeScript 检查
npm run typecheck

# ESLint 检查
npm run lint

# 构建测试
npm run build
```

---

**下次更新**: 修复剩余问题后更新
